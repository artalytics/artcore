% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cdn-helpers.R
\name{has_prefix}
\alias{has_prefix}
\title{Check for any objects under a prefix}
\usage{
has_prefix(bucket, prefix)
}
\arguments{
\item{bucket}{Character(1). Name of the Spaces bucket.}

\item{prefix}{Character(1). The prefix to check (without trailing slash).}
}
\value{
Logical scalar. `TRUE` if one or more objects are found.
}
\description{
Returns `TRUE` if at least one object exists in `bucket` with the given
`prefix` (i.e. any key starts with `prefix/`).
}
\examples{
\dontrun{
has_prefix("art-data", "processed/artistUUID/artUUID")
}
}
